#include <bits/stdc++.h>
using namespace std;

int main()
{
    string s = "Shawon";

    reverse(s.begin(), s.end());

    cout<<s<<endl;

    string s1;
    cin>>s1;
    reverse(s1.begin(),s1.end());
    cout<<s1;
}