#include <bits/stdc++.h>
using namespace std;

int main()
{
    vector<int> v={5,7,3,4,6};

    cout<<v.back()<<endl; //printing the last element of the vector

    v.pop_back(); //removing the last element of the vector

    cout<<v.back()<<endl; //printing the last element of the vector

    v.pop_back(); //removing the last element of the vector
    
    cout<<v.back()<<endl; //printing the last element of the vector
    
    v.pop_back(); //removing the last element of the vector
}